<!doctype html>
<html lang="{{ site.locale }}">
  <head>{% render "head", metadata: site.metadata, title: data.title %}</head>

  <body class="max-w-7xl mx-auto py-8 px-2 md:px-4 lg:px-6 space-y-6">
    {% render "navbar" %}
    <main>{{ content }}</main>
    {% render "footer" %}
  </body>
</html>
